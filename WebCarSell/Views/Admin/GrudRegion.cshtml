@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@using WebCarSell.Models;
@model RegionList

<h1>All Regions</h1>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>        
@foreach (var item in Model.RegionModels) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>          
            <td>
                <a asp-action="EditRegion" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="DeleteRegion" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

